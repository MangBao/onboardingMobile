import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    heart: {
        backgroundColor: '#fff',
        padding: 8,
        borderRadius: 40,
    },

    heartIcon: {
        fontSize: 20,
        color: '#ff3637',
    },
});

export default styles;